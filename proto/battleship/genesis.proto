syntax = "proto3";
package verstakgit.battleship.battleship;

import "gogoproto/gogo.proto";
import "battleship/params.proto";
import "battleship/next_game.proto";
import "battleship/existing_games.proto";
import "battleship/active_games_by_user.proto";
// this line is used by starport scaffolding # genesis/proto/import

option go_package = "github.com/verstakgit/battleship/x/battleship/types";

// GenesisState defines the battleship module's genesis state.
message GenesisState {
  Params params = 1 [(gogoproto.nullable) = false];
  NextGame nextGame = 2;
  repeated ExistingGames existingGamesList = 3 [(gogoproto.nullable) = false];
  repeated ActiveGamesByUser activeGamesByUserList = 4 [(gogoproto.nullable) = false];
  // this line is used by starport scaffolding # genesis/proto/state
}
